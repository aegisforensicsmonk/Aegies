  </div> <!-- closes mainbody -->
</div> <!-- closes main-layout -->
    <footer class="navbar-default navbar-fixed-bottom" style="background:var(--bg-card);border-top:1px solid var(--border-color);">
      <div class="container-fluid text-center footer" style="padding:10px 0;">
        <%
        messages = [
            "<i class='glyphicon glyphicon-flash' style='color:var(--accent-teal);'></i>&nbsp;&nbsp;<a target=_new href='#' style='color:var(--text-muted);'>Welcome to OSINTFOOTPRINTS.</a>",
            "<i class='glyphicon glyphicon-flash' style='color:var(--accent-teal);'></i>&nbsp;&nbsp;<a target=_new href='#' style='color:var(--text-muted);'>OSINTFOOTPRINTS is an open source OSINT automation tool.</a>"
        ]

        import random
        message = messages[random.randint(0, len(messages)-1)]
        %>
        <span style="color:var(--text-muted);font-size:12px;letter-spacing:0.5px;">${message}</span>
      </div>
    </footer>

    </body>
</html>
